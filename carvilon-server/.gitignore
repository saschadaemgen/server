# Build artifacts
bin/
*.exe

# State and runtime data
/state/
/certs/
*.log

# Go workspace
go.work.sum

# IDE
.idea/
.vscode/
*.swp

# OS
Thumbs.db
.DS_Store

# Claude Code local config (deny-rules, permissions)
.claude/

# Lokale Grunddokumentation, NIE ins Repo (echte Anlagen-Daten).
# Die fuenf lebenden Docs (docs/carvilon-server-*.md) und die
# Saison-Protokolle/Uebergaben/Index (seasons/) tragen echte
# Anlagen-Topologie (LAN-IPs, MACs, UUIDs, VPS-IP) und bleiben
# lokal. README.md + NOTICE.md sind generisch und werden getrackt.
# CLAUDE.md liegt aus Tooling-Konvention im Root, ist aber ebenso
# ausgeschlossen.
CLAUDE.md
docs/
seasons/

# systemd EnvironmentFiles with real secrets (the *.env.example
# templates are committed; the filled-in *.env are not).
deploy/systemd/*.env
